@model ActivityManagement.ViewModels.CourseUsersViewModel
@{
    ViewBag.Title = "AddTrainers";
}

<h2>Add a new Trainers</h2>


@using (Html.BeginForm("AddTrainers", "Courses", FormMethod.Post))
{
    <div class="form-group">
        Username:
        @Html.HiddenFor(m => m.CourseId)
        @Html.DropDownListFor(m => m.UserId,
            new SelectList(Model.Users, "Id", "Username"),
            new { @class = "form-control" })
    </div>
    <div class="form-group">
        <button class="btn btn-primary" type="submit">Submit</button>
    </div>
}